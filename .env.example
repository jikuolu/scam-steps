# Public canonical URL for the site. Used in sitemap.xml, robots.txt,
# metadataBase, OpenGraph/Twitter URLs, and per-page canonicals.
# Set per Vercel environment (Production / Preview / Development).
NEXT_PUBLIC_SITE_URL=https://scamsteps.org
